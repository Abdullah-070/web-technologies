<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Auto Care' %></title>
  <link rel="stylesheet" href="/css/style.css">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <nav class="navbar">
    <div class="container nav-container" style="display: flex; justify-content: space-between; align-items: center;">
      <a href="/" class="logo" style="display: flex; align-items: center; gap: 10px; text-decoration: none; color: inherit;">
        <img src="/Assets/logo.png" alt="Auto Care" style="height: 45px; width: auto;">
        <span style="font-size: 1.5rem; font-weight: 700; color: #dc143c;">Auto Care</span>
      </a>
      <button class="hamburger" id="hamburger-menu">&#9776;</button>
      <div class="nav-menu" id="nav-menu">
      <ul class="nav-links" style="display: flex; align-items: center; gap: 1rem; list-style: none; margin: 0 auto; padding: 0;">
        <li><a href="/">Home</a></li>
        <li><a href="/shop">Shop</a></li>
        <li style="position: relative;" class="dropdown">
          <a href="#">Categories &#x25BC;</a>
          <ul class="dropdown-menu">
            <% if (categories && categories.length > 0) { %>
              <% categories.forEach(cat => { %>
                <li><a href="/shop/category/<%= cat.slug %>"><%= cat.name %></a></li>
              <% }) %>
            <% } else { %>
              <li style="padding: 0.25rem 0; color: #777;">No categories</li>
            <% } %>
          </ul>
        </li>
      </ul>
      <ul class="nav-links right-links" style="display: flex; align-items: center; gap: 1rem; list-style: none; margin: 0; padding: 0;">
        <li><a href="/cart">Cart (<%= typeof cartCount !== 'undefined' ? cartCount : 0 %>)</a></li>
        <li><a href="/admin">Admin Panel</a></li>
      </ul>
      </div>
    </div>
  </nav>

  <script>
    // Simple dropdown toggle script
    document.querySelectorAll('.dropdown > a').forEach(el => {
      el.addEventListener('click', function(e) {
        e.preventDefault();
        const menu = this.nextElementSibling;
        menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
      });
    });
    // Close dropdown if clicked outside
    window.addEventListener('click', function(e) {
      if (!e.target.matches('.dropdown > a')) {
        document.querySelectorAll('.dropdown-menu').forEach(menu => {
          menu.style.display = 'none';
        });
      }
    });

    // Hamburger Menu Toggle
    const hamburger = document.getElementById('hamburger-menu');
    const navMenu = document.getElementById('nav-menu');
    hamburger.addEventListener('click', () => {
      navMenu.classList.toggle('active');
    });
    
    // Close menu when clicking a link (optional bonus)
    document.querySelectorAll('.nav-menu a').forEach(link => {
      link.addEventListener('click', () => {
        navMenu.classList.remove('active');
      });
    });
  </script>



  <main class="main-content">
    <%- body %>
  </main>

  <footer class="footer">
    <div class="container">
      <p>&copy; 2026 Auto Care. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
