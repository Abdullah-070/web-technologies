<div class="extracted-style-22">
  <!-- Order Items -->
  <div class="admin-card">
    <h3 class="extracted-style-5">Order Items</h3>
    <table class="admin-table">
      <thead>
        <tr>
          <th>Product</th>
          <th>Price</th>
          <th>Quantity</th>
          <th>Total</th>
        </tr>
      </thead>
      <tbody>
        <% order.items.forEach(item => { %>
          <tr>
            <td>
              <% if (item.product) { %>
                <%= item.product.name %>
              <% } else { %>
                <span class="extracted-style-23">(Product Deleted)</span>
              <% } %>
            </td>
            <td>$<%= item.price.toFixed(2) %></td>
            <td><%= item.quantity %></td>
            <td>$<%= (item.price * item.quantity).toFixed(2) %></td>
          </tr>
        <% }) %>
      </tbody>
      <tfoot>
        <tr class="extracted-style-24">
          <td colspan="3" class="extracted-style-25">Total Amount:</td>
          <td class="extracted-style-26">$<%= order.total.toFixed(2) %></td>
        </tr>
      </tfoot>
    </table>
  </div>

  <!-- Order Info & Status Update -->
  <div>
    <div class="admin-card">
      <h3 class="extracted-style-5">Customer Info</h3>
      <p><strong>Name:</strong> <%= order.user.name %></p>
      <p><strong>Email:</strong> <%= order.user.email %></p>
      
      <h3 class="extracted-style-27">Shipping Info</h3>
      <p><%= order.shippingInfo.address %></p>
      <p><%= order.shippingInfo.city %>, <%= order.shippingInfo.zip %></p>
    </div>

    <div class="admin-card">
      <h3 class="extracted-style-5">Order Status</h3>
      <form action="/admin/orders/status/<%= order._id %>" method="POST">
        <div class="form-group">
          <select name="status" class="form-control">
            <option value="pending" <%= order.status === 'pending' ? 'selected' : '' %>>Pending</option>
            <option value="shipped" <%= order.status === 'shipped' ? 'selected' : '' %>>Shipped</option>
            <option value="delivered" <%= order.status === 'delivered' ? 'selected' : '' %>>Delivered</option>
            <option value="cancelled" <%= order.status === 'cancelled' ? 'selected' : '' %>>Cancelled</option>
          </select>
        </div>
        <button type="submit" class="btn btn-primary extracted-style-28">Update Status</button>
      </form>
    </div>
  </div>
</div>

<div class="extracted-style-29">
  <a href="/admin/orders" class="btn btn-secondary">Back to Orders</a>
</div>
